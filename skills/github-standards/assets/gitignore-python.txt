# Raredon Lab standard .gitignore — Python analysis repositories
# Copy to the repo root as ".gitignore" on first commit.
# Mirrors the R standard; swaps R artifacts for Python/uv equivalents.

# ── Python + environment artifacts ──────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
.python-version        # keep if you pin per-repo; remove this line to commit it
.uv/

# ── Large data objects (never commit) ───────────────────────────────
*.rds
*.h5
*.h5ad
*.h5seurat
*.loom
*.mtx
*.npy
*.npz
*.parquet
*.feather
*.fastq
*.fastq.gz
*.bam
*.bai

# ── Output and data directories (contents excluded) ─────────────────
output/*
!output/.gitkeep
data/*
!data/.gitkeep

# ── OS and editor noise ─────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*~
.vscode/

# ── Credentials — never commit ──────────────────────────────────────
.env
*.env
.envrc
secrets/
