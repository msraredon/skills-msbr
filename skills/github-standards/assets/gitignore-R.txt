# Raredon Lab standard .gitignore — R analysis repositories
# Copy to the repo root as ".gitignore" on first commit.

# ── R session artifacts ─────────────────────────────────────────────
.Rhistory
.RData
.Rproj.user/

# ── Large R data objects (never commit) ─────────────────────────────
*.rds
*.rda
*.RData

# ── Genomics and spatial data files (never commit) ──────────────────
*.h5
*.h5ad
*.h5seurat
*.loom
*.mtx
*.fastq
*.fastq.gz
*.bam
*.bai

# ── Output and data directories (contents excluded) ─────────────────
output/*
!output/.gitkeep
data/*
!data/.gitkeep

# ── OS and editor noise ─────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*~
.vscode/

# ── Credentials — never commit ──────────────────────────────────────
.Renviron
*.env
secrets/
