<!--
Draft this for a lab MEMBER who has scaffolded a project locally and needs the
PI to create the RaredonLab origin (new repos must originate from the org).
Fill placeholders from the scaffolded project, then the member sends it to the PI
(Slack/email). Keep it short and specific.
-->

**Subject:** New RaredonLab repo request — <project-name>

Hi <PI>,

I've scaffolded a new lab project locally and it's ready for a RaredonLab origin.
Could you create the repository?

- **Suggested name:** `RaredonLab/<project-name>`
- **Purpose:** <one sentence>
- **Language / stack:** <R (renv) | Python (uv) | other>
- **Branches:** `main` + `dev`, default `dev` (main protected)
- **Collaborators to add:** <me: username> <, others if known>
- **Structure:** standard lab layout already in place locally
  (`functions/ scripts/ markdowns/ docs/ figures/ output/ data/`, `.gitignore`,
  `README.md`, `CLAUDE.md`).

Once it exists I'll fork it to `<username>/<project-name>-<AB>`, clone, and push
my current work up via a PR to `dev`.

Thanks,
<name> (<AB>)
